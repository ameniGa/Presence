tag: prod

server:
  type: grpc
  host: localhost
  port: 50077
  deadline: 5

database:
  presence:
    type: dynamodb
    userTablename: presence-user-prod
    timeTablename: presence-time-prod

camera:
  deviceID: 0